# Copy this file to .env.local and adjust as needed.
# In development the Vite proxy handles WebSocket routing automatically,
# so you only need this if you're pointing at a remote backend.

# VITE_WS_URL=ws://your-server:4000/ws
